<<<<<<< HEAD
=======
# services:
#     # Default configuration for services
#     _defaults:
#         autowire: true      # Automatically inject dependencies in your services.
#         autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

#     # Makes classes in src/ available to be used as services
#     # This creates a service per class whose id is the fully-qualified class name
#     App\:
#         resource: '../src/'
#         exclude:
#             - '../src/DependencyInjection/'
#             - '../src/Entity/'
#             - '../src/Kernel.php'

#     # Definition for PDO session handler
#     session.handler.pdo:
#         class: Symfony\Component\HttpFoundation\Session\Storage\Handler\PdoSessionHandler
#         arguments:
#             $pdoOrDsn: '@pdo_connection'  # Utiliser le service PDO
#             $options:
#                 db_table: sessions      # Table name for sessions
#                 lock_mode: 0            # Lock mode options to prevent session collisions
#                 use_strict_mode: true   # Enable strict mode to avoid session collisions
#                 cookie_lifetime: 3600   # Session cookie lifetime (in seconds)

#     # Define the PDO service using Doctrine DBAL's native PDO connection
#     pdo_connection:
#         class: PDO
#         factory: ['@doctrine.dbal.default_connection', 'getNativeConnection']  # Get the native PDO connection




















>>>>>>> 4c441433e43898f03584523e21c754dc994edc04
services:
    # Default configuration for services
    _defaults:
        autowire: true      # Automatically inject dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # Makes classes in src/ available to be used as services
    # This creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/DependencyInjection/'
            - '../src/Entity/'
            - '../src/Kernel.php'

    # Definition for PDO session handler
    session.handler.pdo:
        class: Symfony\Component\HttpFoundation\Session\Storage\Handler\PdoSessionHandler
        arguments:
<<<<<<< HEAD
            $pdoOrDsn: '@pdo_connection'  # Utiliser le service PDO
            $options:
                db_table: sessions      # Table name for sessions
                lock_mode: 0            # Lock mode options to prevent session collisions
                use_strict_mode: true   # Enable strict mode to avoid session collisions
                cookie_lifetime: 3600   # Session cookie lifetime (in seconds)
=======
            $pdoOrDsn: '@pdo_connection'  # Use the PDO connection service
            $options:
                db_table: 'sessions'         # Table name for sessions
                db_id_col: 'id'              # ID column for sessions
                db_data_col: 'sess_data'     # Data column for session data
                db_lifetime_col: 'sess_lifetime' # Lifetime column
                db_time_col: 'time'          # Timestamp column for last update
                lock_mode: 0                 # Prevent session collisions
>>>>>>> 4c441433e43898f03584523e21c754dc994edc04

    # Define the PDO service using Doctrine DBAL's native PDO connection
    pdo_connection:
        class: PDO
<<<<<<< HEAD
        factory: ['@doctrine.dbal.default_connection', 'getNativeConnection']  # Get the native PDO connection
=======
        factory: ['@doctrine.dbal.default_connection', 'getNativeConnection']  # Use the native PDO connection
>>>>>>> 4c441433e43898f03584523e21c754dc994edc04
