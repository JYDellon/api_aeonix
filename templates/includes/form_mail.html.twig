{# <!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Formulaire d'envoi de mail</title>
    <link rel="stylesheet" href="/path/to/bootstrap.css"> <!-- Inclure Bootstrap si besoin -->
</head>
<body>
    <div id="mail-form-container" style="display: none; margin-top: 20px;">
        <h2>Envoyer un mail</h2>
        <form id="mail-form" method="post" action="/send-mail">
            <input type="hidden" id="recipients" name="recipients" value="">
            <div class="form-group">
                <label for="mail-subject">Objet</label>
                <input type="text" id="mail-subject" name="subject" class="form-control" placeholder="Objet" required>
            </div>
            <div class="form-group">
                <label for="mail-body">Message</label>
                <textarea id="mail-body" name="body" class="form-control" placeholder="Message" rows="5" required></textarea>
            </div>
            <div>
                <button type="submit" class="btn btn-primary">Envoyer</button>
            </div>
        </form>
    </div>
</body>
</html> #}








<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Envoyer un mail</title>
    <!-- Inclure Bootstrap -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        /* Styles globaux */
        body {
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #2a2f38;
            font-family: 'Arial', sans-serif;
            color: #ffffff;
        }

        /* Conteneur principal */
        .mail-form-container {
            width: 100%;
            max-width: 600px;
            background-color: #1c1f26;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.5);
        }

        /* Titre */
        .mail-form-container h2 {
            text-align: center;
            margin-bottom: 20px;
            font-size: 24px;
            color: #ffffff;
        }

        /* Champs de formulaire */
        .form-control {
            background-color: #2a2f38;
            color: #ffffff;
            border: 1px solid #444;
            border-radius: 5px;
            margin-bottom: 15px;
        }

        .form-control:focus {
            border-color: #6e8efb;
            box-shadow: 0 0 5px rgba(110, 142, 251, 0.5);
        }

        /* Boutons */
        .btn {
            padding: 10px 15px;
            border-radius: 5px;
            font-size: 16px;
        }

        .btn-primary {
            background-color: #007bff;
            color: #ffffff;
        }

        .btn-primary:hover {
            background-color: #0056b3;
        }

        .btn-secondary {
            background-color: #6c757d;
            color: #ffffff;
        }

        .btn-secondary:hover {
            background-color: #5a6268;
        }

        /* Form actions */
        .form-actions {
            display: flex;
            justify-content: space-between;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="mail-form-container">
        <h2>Envoyer un mail</h2>
        <form id="mail-form" method="post" action="/send-mail">
            <!-- Champ destinataires caché -->
            <input type="hidden" id="recipients" name="recipients" value="">

            <!-- Champ Objet -->
            <div class="form-group">
                <label for="mail-subject">Objet</label>
                <input type="text" id="mail-subject" name="subject" class="form-control" placeholder="Objet" required>
            </div>

            <!-- Champ Message -->
            <div class="form-group">
                <label for="mail-body">Message</label>
                <textarea id="mail-body" name="body" class="form-control" placeholder="Message" rows="5" required></textarea>
            </div>

            <!-- Champ pour Pièces jointes -->
            <div class="form-group">
                <label for="mail-attachments">Pièces jointes</label>
                <input type="file" id="mail-attachments" name="attachments[]" class="form-control" multiple>
            </div>

            <!-- Boutons -->
            <div class="form-actions">
                <button type="submit" class="btn btn-primary">Envoyer</button>
                <a href="/" class="btn btn-secondary">Annuler</a>
            </div>
        </form>
    </div>

    <!-- Inclure Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
